# Database Schema & Migrations

This directory contains the MySQL relational database definition and seed datasets:

- `schema.sql`: Complete DDL schema creating tables for `users`, `contacts`, `fingerprints`, `recognition_logs`, `devices`, and `audit_logs`.
- `seed.sql`: Initial administrative user and sample contacts with biometric mappings.
